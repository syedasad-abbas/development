apiVersion: v1
kind: Service
metadata:
  name: nodejs-app
  namespace: voip-test
spec:
  type: ClusterIP
  selector:
    app: nodejs
  ports:
    - name: web
      port: 3000
      targetPort: 3000
      protocol: TCP
